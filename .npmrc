# Ensure consistent package installation
legacy-peer-deps=true
engine-strict=true
prefer-offline=true
frozen-lockfile=true

# Note: Removed deprecated options that were causing warnings
# strict-peer-dependencies and auto-install-peers are no longer supported in modern npm versions
